[project]
name = "screener-v5"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "yfinance>=0.2.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
]

[tool.vercel]
entrypoint = "api.scan:handler"